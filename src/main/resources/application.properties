# Configuración de la base de datos
quarkus.datasource.db-kind = mysql
quarkus.datasource.username = root
quarkus.datasource.password = Dvdshrink2900.
quarkus.datasource.jdbc.url = jdbc:mysql://localhost:3306/almedin

# Configuración de Hibernate ORM
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=true

# Configuración de Panache
quarkus.panache.jdbc.show-sql=true

# Configuración de OpenAPI (Swagger)
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger-ui

# Configuración de JWT
mp.jwt.verify.publickey.location=META-INF/resources/publicKey.pem
quarkus.smallrye-jwt.enabled=true

# Seguridad de Quarkus
quarkus.http.auth.proactive=true